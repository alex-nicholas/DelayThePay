export enum PaymentType {
    DebitCard,
    CreditCard,
    Cash,
    PayPal,
}

export enum PaymentFrequency {
    Weekly,
    Fortnightly,
    Monthly,
}

export enum LaybyType {
    FiftyFifty,
    Installments,
}

export enum LaybyDuration {
    OneMonth,
    TwoMonths,
    ThreeMonths,
    FourMonths,
    FiveMonths,
    SixMonths
}